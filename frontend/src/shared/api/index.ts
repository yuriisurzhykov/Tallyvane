/**
 * Public API of `shared/api` — the generated client and the transport wrapper
 * around it. Empty until the OpenAPI spec exists.
 *
 * The file itself is the point even while empty: in Feature-Sliced Design a
 * segment is reached only through its index, so anything added here is exported
 * deliberately rather than becoming importable by virtue of existing.
 */
export {};
