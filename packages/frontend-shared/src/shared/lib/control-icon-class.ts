/** Tailwind classes that size a Lucide glyph to `--control-icon`. CSS wins over Lucide's default SVG width/height attributes. */
export const CONTROL_ICON_CLASS = "h-(--control-icon) w-(--control-icon)";
