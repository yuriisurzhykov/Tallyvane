/**
 * Public API of the `hero-block` content-block type. Empty until built.
 *
 * Per ADR-013, this one file will hold a schema, field descriptors, defaults
 * and a component — serving the public page render (`frontend`), the admin
 * editor form and live preview (`frontend-admin`), and the JSON Schema the
 * backend validates against, all from one definition.
 */
export {};
