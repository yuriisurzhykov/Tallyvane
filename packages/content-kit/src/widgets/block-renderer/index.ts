/**
 * Public API of `widgets/block-renderer`. Empty until the block registry
 * exists. Resolves a block's `kind` through the registry contract in
 * `frontend-shared/blocks` and must never import a concrete block type
 * directly — see ARCHITECTURE.md §12.6, "the registry as composition root".
 */
export {};
