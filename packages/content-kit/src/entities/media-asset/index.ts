/**
 * Public API of `entities/media-asset`. Empty until the `content` module's
 * media library exists. Lives in `content-kit` for the same reason as
 * `content-page` — both apps read it, only `frontend-admin` writes it.
 */
export {};
