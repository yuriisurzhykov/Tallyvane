/**
 * Public API of `entities/content-page`. Empty until the `content` module's
 * data model exists. Lives in `content-kit`, not in either app's own
 * `entities` layer, because both `frontend` (public render) and
 * `frontend-admin` (editing) need it — see ARCHITECTURE.md §12.5.
 */
export {};
