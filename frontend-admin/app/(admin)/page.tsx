export default function AdminIndexPage() {
    // The root session gate redirects an authorized admin to the admin home.
    // Keeping this page empty prevents the server from redirecting before session checks finish.
    return null;
}