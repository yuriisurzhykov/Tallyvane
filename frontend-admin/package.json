{
  "name": "tallyvane-frontend-admin",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "next dev -p 3200",
    "build": "next build",
    "start": "next start",
    "typecheck": "tsc --noEmit",
    "arch": "pnpm run tokens:check && pnpm run arch:fsd && pnpm run arch:graph",
    "arch:fsd": "steiger ./src",
    "arch:graph": "depcruise app src scripts --config .dependency-cruiser.cjs",
    "graph": "depcruise app src --config .dependency-cruiser.cjs --output-type dot",
    "tokens:generate": "tsx scripts/generate-design-tokens.ts",
    "tokens:check": "tsx scripts/generate-design-tokens.ts --check"
  },
  "devDependencies": {
    "@feature-sliced/steiger-plugin": "^0.7.0",
    "@tailwindcss/postcss": "^4.3.3",
    "@types/node": "^26",
    "@types/react": "^19.2.18",
    "@types/react-dom": "^19.2.4",
    "dependency-cruiser": "^18.2.0",
    "design-token-engine": "workspace:*",
    "postcss": "^8.5.26",
    "steiger": "^0.6.0",
    "tailwindcss": "^4.3.3",
    "tsx": "^4",
    "typescript": "^5"
  },
  "//": "design-token-engine is a devDependency on purpose, same reasoning as frontend-web/package.json: the compiler is build-time only. frontend-shared and content-kit are regular dependencies: their code IS the runtime bundle here. @swc/helpers and @next/env are pinned as direct dependencies to work around a real, currently-open Next.js 16.3.1 bug — see the identical note in frontend-web/package.json for the full explanation and the exact failure each prevents.",
  "dependencies": {
    "@next/env": "16.3.1",
    "@swc/helpers": "0.5.23",
    "content-kit": "workspace:*",
    "frontend-shared": "workspace:*",
    "next": "^16.3.1",
    "react": "^19.2.8",
    "react-dom": "^19.2.8"
  }
}
