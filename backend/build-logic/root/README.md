# root — `tallyvane.root`

`tallyvane.gradle.root.TallyvaneRootPlugin` is the composition root of
build-logic: it applies `ModuleGraphPlugin` and `VerificationPlugin` by
class. The backend root build file names this plugin and nothing else.
